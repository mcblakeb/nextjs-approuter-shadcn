{
  "build": {
    "nixpacksPlan": {
      "phases": {
        "setup": {
          "cmds": ["npm ci --legacy-peer-deps --force || npm install --legacy-peer-deps --force"]
        },
        "build": {
          "cmds": ["npm run build"]
        }
      }
    }
  }
}